/**
 * `EQUIPEMENT_OPTIONS` vit désormais dans @app/shared (voir types.ts pour la même remarque sur
 * `Equipement`) — ré-exporté ici pour ne pas casser les imports existants depuis @app/annonces-core.
 */
export { EQUIPEMENT_OPTIONS } from '@app/shared';
