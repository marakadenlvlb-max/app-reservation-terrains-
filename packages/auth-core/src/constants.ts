/**
 * `SPORT_OPTIONS` vit désormais dans @app/shared (voir types.ts pour la même remarque sur
 * `Sport`) — ré-exporté ici pour ne pas casser les imports existants depuis @app/auth-core.
 */
export { SPORT_OPTIONS } from '@app/shared';
