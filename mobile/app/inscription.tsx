import { RegisterScreen } from '../src/modules/authentification/screens/RegisterScreen';

// Écran d'inscription (US-01) — reste fin, comme les autres routes.
export default function InscriptionRoute() {
  return <RegisterScreen />;
}
