import { LoginScreen } from '../src/modules/authentification/screens/LoginScreen';

// Écran de connexion (US-02) — reste fin, comme les autres routes.
export default function ConnexionRoute() {
  return <LoginScreen />;
}
